First modify the executable_path in Bloomindale and Sephora to the place the chromedriver locate at.
Directly run main.py and the http://127.0.0.1:5000/ will be the root.
The methods for the root page is POST so expect to receieve the JSON format of {'KEY': "smaple_key"}
and it will return the result in JSON format.
